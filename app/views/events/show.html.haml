.row
  .col-md-9
    %h3
      = @event.name
      %small= @event.starts_at.to_s(:long)

    %p= @event.location
    %p= markdownify(@event.description)

  .col-md-3
    %ul.nav.nav-pills.nav-stacked
      %li= link_to "Count me in", "#", class: "btn btn-default"

